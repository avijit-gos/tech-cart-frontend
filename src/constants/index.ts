/** @format */

// Pubsub Topics
export const VITE_API_URL = import.meta.env.VITE_API_URL;
export const VITE_IMAGE_URL = import.meta.env.VITE_IMAGE_URL;

export const urls = Object.freeze({
  BANNER_ENDPOINT: "/banner/active",
});
export const PUBSUB_TOPICS = {
  TOPIC_SIGNUP: "signup",
  TOPIC_SIGNIN: "signin",
};
